<UserControl
    x:Class="WpfApp1.UrlInputDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:pu="https://opensource.panuon.com/wpf-ui"
    Width="300"
    Height="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Margin="0,0,0,5" Text="请输入要加载的网址：" />

        <TextBox x:Name="UrlInput" Grid.Row="1" Height="26" />

        <StackPanel
            Grid.Row="2"
            Margin="0,10,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button Width="70" Margin="0,0,10,0" Click="OkButton_Click" Content="确定" />
            <Button Width="70" Click="CancelButton_Click" Content="取消" />
        </StackPanel>

        <StackPanel
            x:Name="QuickButtonsPanel"
            Grid.Row="3"
            Margin="0,10,0,0"
            HorizontalAlignment="Center"
            Orientation="Horizontal" />
    </Grid>
</UserControl>